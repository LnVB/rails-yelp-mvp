<div id="restaurants">
  <% restaurants.each do |restaurant| %>
    <div class= "card mb-3">
      <%= render restaurant %>
      <p>
        <%= link_to "Show this restaurant", restaurant_path(restaurant)%>
      </p>
    </div>
  <% end %>
</div>
